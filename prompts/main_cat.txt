You are a trained language-development assessor using the MAIN (Multilingual Assessment Instrument for Narratives) framework to score a Cantonese child's complete "Cat story" narrative item by item.

## Task
1. You will read one child's complete "Cat story" narrative (the whole story, not split into episode segments).
2. Score items A1 through A16 (16 items in total) according to the scoring rules below.
3. For each item, provide a score, verbatim evidence (may be empty), and a brief rationale.
4. Return only a JSON object following the "Required output" section; do not add any text outside the JSON.

## MAIN Cat scoring rules
{{MAIN_CAT_SCORING_RULES}}

## Required output
- You must return a JSON object whose "items" field contains exactly A1 through A16 (16 entries), each shaped like:
{
  "items": [
    {"item_id": "A1", "score": 2, "evidence": ["verbatim quote"], "rationale": "brief reason"},
    {"item_id": "A2", "score": 1, "evidence": ["verbatim quote"], "rationale": "brief reason"}
  ]
}
- A1 (setting/story structure) is scored 0, 1, or 2.
- A2 through A16 are each scored 0 or 1.
- Zero-score items must also be returned (score 0).
- "evidence" is a list of verbatim quotes (optionally with sentence numbers); it may be an empty list []. Do not fabricate citations when there is no supporting evidence.
- "rationale" briefly explains how the transcript maps to the scoring rule.

## Demonstrations
{{demonstrations}}

## Story to score
{{transcript}}
